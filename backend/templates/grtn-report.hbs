<html>
<head>
  <style>
    :root { --grtn-orange: #ea580c; --slate-800: #1e293b; --text: #334155; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; padding: 40px; color: var(--text); line-height: 1.5; }
    
    .header-table { width: 100%; border-bottom: 4px solid var(--grtn-orange); margin-bottom: 25px; }
    .company-name { font-size: 28px; font-weight: 800; color: var(--slate-800); letter-spacing: -1px; }
    .document-type { color: var(--grtn-orange); font-size: 14px; font-weight: bold; text-transform: uppercase; }

    .info-section { display: flex; justify-content: space-between; margin-bottom: 30px; gap: 20px; }
    .info-box { flex: 1; background: #fff7ed; padding: 15px; border-radius: 8px; border: 1px solid #ffedd5; font-size: 12px; }
    .info-box strong { color: var(--grtn-orange); font-size: 11px; text-transform: uppercase; display: block; margin-bottom: 5px; }

    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th { background: var(--slate-800); color: white; padding: 12px; text-align: left; font-size: 11px; text-transform: uppercase; letter-spacing: 1px; }
    td { padding: 12px; border-bottom: 1px solid #e2e8f0; font-size: 13px; }
    
    .total-section { margin-top: 30px; display: flex; justify-content: flex-end; }
    .total-table { width: 250px; }
    .total-row { display: flex; justify-content: space-between; padding: 10px 0; border-top: 2px solid var(--slate-800); font-weight: bold; font-size: 16px; }

    .footer { position: fixed; bottom: 30px; left: 40px; right: 40px; border-top: 1px solid #e2e8f0; pt: 10px; display: flex; justify-content: space-between; font-size: 10px; color: #94a3b8; }
  </style>
</head>
<body>

  <table class="header-table">
    <tr>
      <td>
        <div class="company-name">{{companyName}}</div>
        <div class="document-type">Goods Return Note</div>
      </td>
      <td style="text-align: right;">
        <h2 style="margin: 0; color: var(--grtn-orange);">{{grtn.returnNumber}}</h2>
        <p style="margin: 0; font-size: 12px; color: #64748b;">Date: {{grtn.returnDate}}</p>
      </td>
    </tr>
  </table>

  <div class="info-section">
    <div class="info-box">
        <strong>Return Information</strong>
        Processed By: {{grtn.returnedBy}}<br>
        Reason: {{grtn.returnReason}}<br>
        Ref GRN: {{grtn.grnId.grnNumber}}
    </div>
    <div class="info-box">
        <strong>Supplier Details</strong>
        {{grtn.grnId.purchaseOrderId.supplier.contactInformation.primaryContactName}}<br>
        {{grtn.grnId.purchaseOrderId.supplier.contactInformation.emailAddress}}<br>
        {{grtn.grnId.purchaseOrderId.supplier.contactInformation.phoneNumber}}
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th width="40%">Item Description / Notes</th>
        <th width="20%" style="text-align: center;">Return Qty</th>
        <th width="20%" style="text-align: right;">Total Value</th>
        <th width="20%" style="text-align: center;">Status</th>
      </tr>
    </thead>
    <tbody>
      {{#each grtn.items}}
      <tr>
        <td>
          <div style="font-weight: bold;">Line Item #{{@index}}</div>
          <small style="color: #64748b;">{{this.itemsNotes}}</small>
        </td>
        <td style="text-align: center; font-weight: 600;">{{this.returnQty}}</td>
        <td style="text-align: right;">${{this.totalAmount}}</td>
        <td style="text-align: center;">
            <span style="background: #f1f5f9; padding: 4px 8px; border-radius: 4px; font-size: 10px; text-transform: uppercase;">{{this.status}}</span>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <div class="total-section">
    <div class="total-table">
        <div class="total-row">
            <span>TOTAL REFUND</span>
            <span style="color: var(--grtn-orange);">${{grtn.totalAmountCalculated}}</span>
        </div>
    </div>
  </div>

  <div style="margin-top: 40px; font-size: 12px; background: #f8fafc; padding: 15px; border-left: 4px solid #cbd5e1;">
    <strong>Additional Notes:</strong><br>
    {{#if grtn.notes}}{{grtn.notes}}{{else}}No additional return instructions provided.{{/if}}
  </div>

  <div class="footer">
    <span>Generated by Humber Mobility Scooter WMS</span>
    <span>Page 1 of 1</span>
  </div>

</body>
</html>